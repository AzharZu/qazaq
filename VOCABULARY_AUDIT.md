# 📋 АУДИТ СЛОВАРЯ - ПОЛНЫЙ АНАЛИЗ

**Дата:** 19 декабря 2025  
**Статус:** ❌ ТРЕБУЕТ ИСПРАВЛЕНИЙ

---

## ✅ ЧТО РАБОТАЕТ ПРАВИЛЬНО

### Backend - Логика добавления слов

1. **`vocabulary_service.sync_lesson_vocabulary()`** ✅
   - Вызывается ТОЛЬКО при открытии урока (не в preview)
   - Добавляет слова ИЗ:
     - Блоков типа `pronunciation`, `audio_task`
     - Флэшкарточек из `lesson.flashcards`
   - Проверяет дубликаты по `(user_id, word.lower())`
   - Не удаляет старые слова
   - **Логика:** ✅ ПРАВИЛЬНАЯ

2. **`/api/dictionary`** (Backend) ✅
   - Возвращает ВСЕ слова пользователя
   - Включает прогресс (`success`, `fails`)
   - Фильтра по уроку нет (правильно!)
   - **Реализация:** ✅ ПРАВИЛЬНАЯ

---

## ❌ ОШИБКИ И ПРОБЛЕМЫ

### Frontend - СТРУКТУРНЫЕ ОШИБКИ

#### 1. **Двойное/тройное отображение слов** ❌

**Проблема:** Фронт смешивает слова из РАЗНЫХ источников:

- **`/dictionary/index.tsx`** - ПЕРВЫЙ источник
  - `useDictionaryWords()` - получает из `/api/dictionary`
  - Показывает слова как карточки (flip-card UI)

- **`useDictionaryStore()`** в lesson.tsx - ВТОРОЙ источник
  - `loadWords()` - копирует слова из урока в локальный state
  - Может создать ДУБЛИ

**Файлы:**
```
frontend/src/pages/dictionary/index.tsx        - основное отображение
frontend/src/store/dictionaryStore.ts          - локальное хранилище
frontend/src/pages/lesson/[id].tsx             - синк в уроке
```

**Последствие:** Одно слово может отобразиться 2-3 раза

**Решение:** ❌ УДАЛИТЬ локальное хранилище, использовать ТОЛЬКО `/api/dictionary`

---

#### 2. **useDictionaryStore() - АНТИПАТТЕРН** ❌

**Файл:** `frontend/src/store/dictionaryStore.ts`

**Проблема:** 
- Дублирует состояние из backend
- Может десинхронизироваться с `/api/dictionary`
- Хранит слова ЛОКАЛЬНО (в памяти + localStorage)
- Создает второй "словарь" внутри урока

**Используется в:**
```
lesson/[id].tsx              - loadWords() при открытии урока
dictionary/choose.tsx        - submitResult()
dictionary/write.tsx         - markSuccess(), markFail()
components/DictionaryPractice.tsx
```

**Должно быть:**
- ❌ Удалить `dictionaryStore.ts`
- ❌ Не вызывать `loadWords()` из урока
- ✅ Использовать ТОЛЬКО прямые запросы к `/api/dictionary`

---

#### 3. **Неправильная синхронизация в `lesson/[id].tsx`** ❌

**Файл:** `frontend/src/pages/lesson/[id].tsx`

**Проблема (строки):**
```typescript
const { loadWords } = useDictionaryStore();  // ❌ Не нужно!

// ... в useEffect:
// ❌ Вероятно: loadWords() вызывается здесь
```

**Должно быть:**
- ❌ Удалить импорт `useDictionaryStore`
- ❌ Удалить `loadWords()` из useEffect
- ❌ Удалить отслеживание `newWordsAdded`
- ✅ Backend уже синкирует слова автоматически

---

#### 4. **Несовместимые UI компоненты** ❌

**Проблема:** Есть ДВА компонента для флэшкарточек:

- **`Flashcard.tsx`** - для УРОКОВ
  - Отображает `front` / `back` из блоков урока
  - Локальное состояние (не синкируется)

- **`DictionaryFlashcard.tsx`** - для СЛОВАРЯ
  - Отображает `word` / `translation` из словаря
  - Должно синкировать прогресс в backend

**Ошибка:** `Flashcard.tsx` НЕ должен добавлять слова в словарь!
- Это работа BACKEND при открытии урока
- Frontend только ОТОБРАЖАЕТ блоки

**Решение:** 
- ❌ `Flashcard.tsx` - оставить как есть (для уроков)
- ❌ Не вызывать из него синк словаря
- ✅ `DictionaryFlashcard.tsx` - использовать ТОЛЬКО для `/dictionary`

---

### Backend - ПОТЕНЦИАЛЬНЫЕ ПРОБЛЕМЫ

#### 5. **Возможный баг с `flashcards` и `word_ids`** ⚠️

**Файл:** `backend/app/api/routes/lessons.py` (строка ~174)

**Проблема:** 
- Блок типа `flashcard` может использовать `word_ids` из словаря
- Блок типа `flashcards` использует встроенные `lesson.flashcards`
- Возможна путаница между этими двумя типами

**Вопрос для clarify:**
- `word_ids` блок - это ID из ОСНОВНОГО словаря?
- Или это отдельный тип блока?

**Действие:** Проверить логику в `extract_words_from_blocks()`

---

#### 6. **Логирование `VOCAB_SYNC_START`** ⚠️

**Файл:** `backend/app/services/vocabulary_service.py` (строка ~201)

**Проблема:** 
```python
logger.info("VOCAB_SYNC_START", user_id=user_id, lesson_id=lesson.id)
```

Это не стандартный способ логирования. Должно быть:
```python
logger.info(f"VOCAB_SYNC_START: user_id={user_id}, lesson_id={lesson.id}")
```

---

## 🧪 ТЕСТОВЫЕ СЦЕНАРИИ - ТЕКУЩЕЕ СОСТОЯНИЕ

### Сценарий 1: Открыть урок с флэшкарточками

**Ожидается:**
1. Backend вызывает `sync_lesson_vocabulary()` ✅
2. Слова добавляются в `vocabulary_words` таблицу ✅
3. Frontend отображает флэшкарточки В УРОКЕ ✅

**Проблема:** 
- ❌ Frontend может вызвать `loadWords()` → ДУБЛЬ в памяти
- ❌ `useDictionaryStore` создает локальное состояние

---

### Сценарий 2: Открыть Словарь

**Ожидается:**
1. GET `/api/dictionary` возвращает ВСЕ слова ✅
2. Отображаются как карточки ✅
3. Упражнения обновляют прогресс (`correct_attempts`, `wrong_attempts`) ✅

**Проблема:** 
- ❓ Смешиваются ли слова из `/api/dictionary` со словами из `useDictionaryStore`?
- ❓ Есть ли дубли в отображении?

---

### Сценарий 3: Два урока с одинаковыми словами

**Ожидается:**
1. Слова из урока 1 → добавлены в словарь ✅
2. Слова из урока 2 → если дубль, НЕ добавлять ✅
3. Словарь содержит каждое слово ОДИН РАЗ ✅

**Проблема:** 
- ⚠️ Может быть дублирование если `useDictionaryStore` не синхронизирован

---

## 🚀 ПЛАН ИСПРАВЛЕНИЙ (ПРИОРИТЕТ)

### КРИТИЧНЫЕ (БЛОКИРУЮТ)

**1. Удалить `dictionaryStore.ts`** 🔴
   - Файл: `/frontend/src/store/dictionaryStore.ts`
   - Альтернатива: Прямые запросы `useDictionaryWords()`

**2. Очистить импорты `useDictionaryStore`** 🔴
   - `lesson/[id].tsx` - удалить импорт и вызовы
   - `dictionary/choose.tsx` - удалить `submitResult()`
   - `dictionary/write.tsx` - удалить `markSuccess()`, `markFail()`

**3. Не вызывать синк из lesson.tsx** 🔴
   - Backend уже это делает!
   - Frontend не должен трогать vocabulary_service

---

### ВАЖНЫЕ (КАЧЕСТВО КОДА)

**4. Унифицировать UI словаря** 🟡
   - Использовать `DictionaryFlashcard.tsx` везде в `/dictionary`
   - Удалить `Flashcard.tsx` из словаря (оставить только для уроков)

**5. Логирование в backend** 🟡
   - Исправить `logger.info()` вызовы
   - Стандартизировать формат логов

---

### ОПЦИОНАЛЬНЫЕ (ОПТИМИЗАЦИЯ)

**6. Кэширование `/api/dictionary`** 🟢
   - React Query / SWR для автообновления
   - Оптимистичные обновления при изменении прогресса

**7. Pagination для большого словаря** 🟢
   - Если > 1000 слов, использовать pagination

---

## ✅ ФИНАЛЬНЫЙ ЧЕКЛИСТ ГОТОВНОСТИ

- [ ] `dictionaryStore.ts` удален
- [ ] Все импорты `useDictionaryStore` удалены
- [ ] `lesson/[id].tsx` не вызывает синк словаря
- [ ] Словарь показывает слова ОДИН РАЗ
- [ ] Два урока с одинаковыми словами → нет дублей
- [ ] Прогресс синкируется правильно
- [ ] Backend логирование унифицировано
- [ ] Нет console.log ошибок в frontend
- [ ] Нет 500 ошибок в backend при синке

---

## 📝 РЕЗЮМЕ

| Компонент | Статус | Действие |
|-----------|--------|----------|
| Backend sync | ✅ Работает | Логирование унифицировать |
| `/api/dictionary` | ✅ Работает | Добавить кэширование |
| Frontend UI словаря | ⚠️ Работает с ошибками | Удалить dictionaryStore |
| Синх из уроков | ❌ Конфликт логики | Удалить loadWords() |
| Отображение | ❌ Потенциальные дубли | Унифицировать компоненты |

**ИТОГ:** Архитектура хорошая, но реализация на frontend нарушена.  
Нужно **ПОЛНОСТЬЮ избавиться от `dictionaryStore.ts`** и использовать ТОЛЬКО backend API.
