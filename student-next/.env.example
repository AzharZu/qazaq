# Next.js Environment Configuration for Student Application

# API URL - Use relative path for Docker (proxied by Nginx)
# For development: http://localhost:8000/api
# For production: /api (via Nginx reverse proxy)
NEXT_PUBLIC_API_URL=/api

# Node environment
NODE_ENV=production

# Next.js configuration
NEXT_PUBLIC_APP_NAME=Qazaq Student
