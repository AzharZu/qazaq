# 🐛 НАЙДЕННЫЕ ОШИБКИ - ДЕТАЛЬНЫЙ СПИСОК

**Дата:** 19 декабря 2025  
**Статус:** Готово к исправлению

---

## 📊 СТАТИСТИКА

- **Критичные ошибки:** 5
- **Важные:** 3
- **Опциональные:** 2
- **Всего файлов с ошибками:** 8

---

## 🔴 КРИТИЧНЫЕ ОШИБКИ (MUST FIX)

### ОШИБКА #1: `loadWords()` вызывается в lesson/[id].tsx ❌

**Файл:** `/student-next/src/pages/lesson/[id].tsx`  
**Строки:** 31, 60, 73, 126  
**Severity:** 🔴 БЛОКИРУЕТ

**Проблема:**
```typescript
// ❌ СТРОКА 31
const { loadWords } = useDictionaryStore();

// ❌ СТРОКА 60
if (!previewMode) {
  setNewWordsAdded(detail?.new_words_added || 0);
  await loadWords().catch(() => {});  // ← ДВОЙНОЕ ДОБАВЛЕНИЕ!
}

// ❌ СТРОКА 73
}, [id, previewMode, reset, setLesson, normalizeType, loadWords]);

// ❌ СТРОКА 126
await loadWords().catch(() => {});  // ← ДВА РАЗА!
```

**Последствие:**
1. Backend добавляет слова в БД при открытии урока ✅
2. Frontend ДОПОЛНИТЕЛЬНО вызывает `loadWords()` ❌
3. В памяти создается локальная копия словаря ❌
4. Если пользователь открыл урок → словарь в памяти, но потом открыл другой урок → может быть несинхронизация ❌

**Решение:**
- ❌ Удалить импорт: `import { useDictionaryStore }`
- ❌ Удалить строку 31: `const { loadWords } = useDictionaryStore();`
- ❌ Удалить строки 60: `await loadWords().catch(() => {});`
- ❌ Удалить строки 73: убрать `loadWords` из зависимостей
- ❌ Удалить строки 126: `await loadWords().catch(() => {});`

**Код для замены:**
```typescript
// БЫЛО:
const { loadWords } = useDictionaryStore();

// СТАЛО:
// ← НИЧЕГО! Backend уже сделал sync

// ===

// БЫЛО:
if (!previewMode) {
  setNewWordsAdded(detail?.new_words_added || 0);
  await loadWords().catch(() => {});
}

// СТАЛО:
if (!previewMode) {
  setNewWordsAdded(detail?.new_words_added || 0);
  // loadWords() НЕ вызываем! Backend уже добавил
}

// ===

// БЫЛО:
}, [id, previewMode, reset, setLesson, normalizeType, loadWords]);

// СТАЛО:
}, [id, previewMode, reset, setLesson, normalizeType]);

// ===

// БЫЛО (СТРОКА 126):
await loadWords().catch(() => {});

// СТАЛО:
// ← УДАЛИТЬ (backend handle это автоматически)
```

---

### ОШИБКА #2: `useDictionaryStore` антипаттерн ❌

**Файл:** `/student-next/src/store/dictionaryStore.ts`  
**Severity:** 🔴 АРХИТЕКТУРНАЯ

**Проблема:**
Это хранилище дублирует состояние backend и создает ВТОРОЙ "словарь":
- Может десинхронизироваться с БД
- Хранит состояние в памяти (браузер) и localStorage
- Используется из нескольких мест (conflicting updates)

**Используется в:**
- `lesson/[id].tsx` - loadWords() при открытии ❌
- `DictionaryPractice.tsx` - loadWords(), markSuccess(), markFail() ✅ (нужно здесь)
- `Navbar.tsx` - loadWords() ❌
- `useDictionaryWords.ts` - loadWords() ✅ (может быть нужно)
- `dictionary/repeat.tsx` - submitResult() ✅ (нужно здесь)

**Решение (2 варианта):**

**Вариант A (Рекомендуется):** Оставить store, но ПЕРЕДЕЛАТЬ logику
- ✅ Оставить `dictionaryStore.ts` для упражнений (markSuccess, markFail, submitResult)
- ✅ Но `loadWords()` должна ТОЛЬКО в dictionary компонентах
- ❌ Удалить из `lesson/[id].tsx`
- ❌ Удалить из `Navbar.tsx`

**Вариант B:** Полностью удалить store
- ❌ Удалить `dictionaryStore.ts`
- ❌ Заменить `useSWR('/api/dictionary/...}')`
- Требует переделки всех компонентов

**Рекомендую:** Вариант A (минимальные изменения)

---

### ОШИБКА #3: `useDictionaryWords.ts` неправильно реализован ❌

**Файл:** `/student-next/src/lib/useDictionaryWords.ts`  
**Severity:** 🔴 ФУНКЦИОНАЛЬНАЯ

**Проблема:**
```typescript
// ТЕКУЩИЙ КОД:
import { useDictionaryStore } from "@/store/dictionaryStore";

export function useDictionaryWords() {
  const { words, loadWords, loading } = useDictionaryStore();
  
  useEffect(() => {
    loadWords();
  }, [loadWords]);
  
  return { words, loading };
}
```

**Последствие:**
- Вызывает `loadWords()` каждый раз когда хук используется
- Может вызывать множественные API запросы

**Правильная реализация:**
```typescript
// ПРАВИЛЬНО:
import { useEffect, useState } from "react";
import { dictionaryApi } from "@/lib/api/dictionary";

export function useDictionaryWords() {
  const [words, setWords] = useState([]);
  const [loading, setLoading] = useState(true);
  const [error, setError] = useState<string | null>(null);
  
  useEffect(() => {
    const load = async () => {
      try {
        const data = await dictionaryApi.getDictionaryWords();
        setWords(data);
      } catch (err) {
        setError((err as any)?.response?.data?.detail || "Error");
      } finally {
        setLoading(false);
      }
    };
    
    load();
  }, []); // ← Только один раз при монте
  
  return { words, loading, error };
}
```

---

### ОШИБКА #4: `Navbar.tsx` вызывает loadWords() ❌

**Файл:** `/student-next/src/components/Navbar.tsx`  
**Строки:** 19, 28, 30  
**Severity:** 🔴 НЕПРАВИЛЬНАЯ ЛОГИКА

**Проблема:**
```typescript
// ❌ НЕПРАВИЛЬНО:
const { words, loadWords } = useDictionaryStore();

useEffect(() => {
  if (token) {
    loadWords().catch(() => {});  // ← Почему в navbar?
  }
}, [token, loadWords]);
```

**Последствие:**
- Navbar загружает словарь при логине/разлогине
- Но это должно происходить в компоненте, который его ИСПОЛЬЗУЕТ
- Создает ненужный запрос

**Решение:**
- ❌ Удалить `loadWords()` из Navbar
- ✅ Оставить отображение счетчика слов (если есть)

```typescript
// БЫЛО:
const { words, loadWords } = useDictionaryStore();

useEffect(() => {
  if (token) {
    loadWords().catch(() => {});
  }
}, [token, loadWords]);

// СТАЛО:
const { words } = useDictionaryStore();

// loadWords() удален! Он вызывается там, где используется
```

---

### ОШИБКА #5: Дублирование при смене уроков ❌

**Файл:** `/student-next/src/pages/lesson/[id].tsx` (70-73 строки)  
**Severity:** 🔴 DATA CONSISTENCY

**Проблема:**
```typescript
// ❌ Два вызова loadWords() при открытии урока:
// 1) Строка 60: await loadWords().catch(() => {});
// 2) Строка 126: await loadWords().catch(() => {}); 

// Плюс потенциально еще один при useEffect cleanup
```

**Последствие:**
- При открытии урока может быть 2-3 запроса вместо 0
- Словарь грузится тогда, когда НЕ нужно

**Решение:**
- ❌ Удалить оба вызова loadWords()
- Backend уже сделал sync

---

### ОШИБКА #9: `profile/index.tsx` вызывает loadWords() ❌

**Файл:** `/student-next/src/pages/profile/index.tsx`  
**Строки:** 31, 69-70  
**Severity:** 🔴 НЕПРАВИЛЬНАЯ ЛОГИКА

**Проблема:**
```typescript
// ❌ СТРОКА 31:
const { words, loadWords, markSuccess } = useDictionaryStore();

// ❌ СТРОКА 69-70:
useEffect(() => {
  if (!token) return;
  loadWords().catch(() => {});
}, [token, loadWords]);
```

**Последствие:**
- Profile загружает словарь, когда в него заходят
- Это создает ненужный API запрос
- Словарь должен загружаться только в компоненте, который его ИСПОЛЬЗУЕТ

**Используется для:**
- `wordOfWeek` - отображение слова недели (может быть разумно)
- Но `loadWords()` используется слишком часто

**Решение:**
- Если `wordOfWeek` нужен из backend, то получать его прямым запросом
- ❌ НЕ использовать `useDictionaryStore` для получения wordOfWeek

```typescript
// БЫЛО:
const { words, loadWords, markSuccess } = useDictionaryStore();

useEffect(() => {
  if (!token) return;
  loadWords().catch(() => {});
}, [token, loadWords]);

// СТАЛО (если нужен wordOfWeek):
const [wordOfWeek, setWordOfWeek] = useState<VocabularyWord | null>(null);

useEffect(() => {
  if (!token) return;
  const fetchWordOfWeek = async () => {
    try {
      const word = await dictionaryApi.getWordOfWeek();
      setWordOfWeek(word);
    } catch (err) {
      console.error("Failed to fetch word of week", err);
    }
  };
  fetchWordOfWeek();
}, [token]);
```

---

### ОШИБКА #10: Неправильная логика loadWords в DictionaryPractice.tsx ❌

**Файл:** `/student-next/src/components/DictionaryPractice.tsx`  
**Строки:** 15-19  
**Severity:** 🔴 МНОЖЕСТВЕННЫЕ ЗАПРОСЫ

**Проблема:**
```typescript
// ❌ НЕПРАВИЛЬНО:
useEffect(() => {
  if (!words.length) {
    loadWords();  // ← Может быть вызвано много раз!
  }
}, [loadWords, words.length]);

// Проблема: если words.length === 0 → вызвать loadWords
// Но когда loadWords завершится, words.length изменится → useEffect не запустится
// Если же что-то еще вызовет loadWords → будет дублирование
```

**Последствие:**
- При открытии страницы может быть несколько запросов подряд
- Race condition если быстро переключаться между страницами

**Решение:**
```typescript
// ✅ ПРАВИЛЬНО:
const [loaded, setLoaded] = useState(false);

useEffect(() => {
  if (!loaded && words.length === 0) {
    loadWords().then(() => setLoaded(true));
  }
}, []); // ← Только один раз при монтировании!
```

---

## 🟡 ВАЖНЫЕ ОШИБКИ (SHOULD FIX)

### ОШИБКА #6: `DictionaryPractice.tsx` неправильно загружает ❌

**Файл:** `/student-next/src/components/DictionaryPractice.tsx`  
**Severity:** 🟡 МОЖНО ОПТИМИЗИРОВАТЬ

**Проблема:**
```typescript
// Вероятно, что-то типа:
const { words, loadWords } = useDictionaryStore();

useEffect(() => {
  loadWords();
}, [loadWords, words.length]);
```

**Решение:**
- Загружать слова ОДИН РАЗ при монтировании компонента
- Не перезагружать при изменении length

---

### ОШИБКА #7: Backend логирование неправильное ⚠️

**Файл:** `backend/app/services/vocabulary_service.py`  
**Severity:** 🟡 КАЧЕСТВО КОДА

**Проблема:**
```python
# Неправильно:
logger.info("VOCAB_SYNC_START", user_id=user_id, lesson_id=lesson.id)
```

**Правильно:**
```python
# ✅ Вариант 1 (рекомендуется):
logger.info(f"VOCAB_SYNC_START: user_id={user_id}, lesson_id={lesson.id}")

# ✅ Вариант 2 (через dict):
logger.info("VOCAB_SYNC_START", extra={"user_id": user_id, "lesson_id": lesson.id})
```

---

### ОШИБКА #8: Нет проверки `preview` при получении `new_words_added` ⚠️

**Файл:** `/student-next/src/pages/lesson/[id].tsx` (строка 59-61)  
**Severity:** 🟡 ЛОГИКА МОЖЕТ БЫТЬ НЕПРАВИЛЬНОЙ

**Проблема:**
```typescript
// ❓ Неясно: backend возвращает new_words_added в обоих режимах?
// Если в preview режиме backend НЕ синкирует слова,
// то new_words_added должна быть 0

if (!previewMode) {
  setNewWordsAdded(detail?.new_words_added || 0);
}
```

**Решение:**
- ✅ Убедиться, что backend НЕ возвращает new_words_added в preview
- ✅ Или явно устанавливать 0 для preview

---

## 🟢 ОПЦИОНАЛЬНЫЕ УЛУЧШЕНИЯ (NICE TO HAVE)

### УЛУЧШЕНИЕ #1: Кэширование GET /api/dictionary

**Текущий подход:** Запрос к API каждый раз  
**Рекомендация:** Использовать React Query или SWR

```typescript
// Пример с SWR:
import useSWR from 'swr';

export function useDictionaryWords() {
  const { data: words = [], isLoading, error } = useSWR(
    '/api/dictionary',
    (url) => fetch(url).then(r => r.json())
  );
  
  return { words, loading: isLoading, error };
}
```

---

### УЛУЧШЕНИЕ #2: Профилактика race conditions

**Текущая проблема:** Если пользователь очень быстро открывает два урока подряд, может быть race condition

**Рекомендация:** Использовать AbortController

```typescript
useEffect(() => {
  const controller = new AbortController();
  
  const load = async () => {
    const detail = await lessonsApi.getLesson(id as string, {
      preview: previewMode,
      signal: controller.signal
    });
    setLessonDetail(detail);
  };
  
  load().catch(err => {
    if (err.name !== 'AbortError') console.error(err);
  });
  
  return () => controller.abort();
}, [id, previewMode]);
```

---

## ✅ ЧЕКЛИСТ ИСПРАВЛЕНИЙ

**Приоритет 1 (Критично):**
- [ ] Удалить `loadWords()` из `lesson/[id].tsx` (строки 31, 60, 73, 126)
- [ ] Удалить `loadWords()` из `Navbar.tsx` (строки 19, 28, 30)
- [ ] Удалить `loadWords()` из `profile/index.tsx` (строки 31, 69-70)
- [ ] Переделать `useDictionaryWords.ts` (использовать прямой API запрос)
- [ ] Удалить дублирование `loadWords()` вызовов
- [ ] Проверить `DictionaryPractice.tsx` логику загрузки (строки 15-19)

**Приоритет 2 (Важно):**
- [ ] Исправить логирование в backend (vocabulary_service.py)
- [ ] Убедиться, что preview режим не синкирует слова
- [ ] Добавить обработку ошибок

**Приоритет 3 (Опционально):**
- [ ] Добавить кэширование (SWR/React Query)
- [ ] Защита от race conditions (AbortController)

---

## 📝 РЕЗЮМЕ ИСПРАВЛЕНИЙ ПО ФАЙЛАМ

| Файл | Ошибки | Действие |
|------|--------|----------|
| `lesson/[id].tsx` | #1, #5 | Удалить 4 вызова loadWords() |
| `Navbar.tsx` | #4 | Удалить loadWords() логику |
| `profile/index.tsx` | #9 | Удалить loadWords() и переделать wordOfWeek |
| `useDictionaryWords.ts` | #3 | Переделать полностью |
| `dictionaryStore.ts` | #2 | Оставить, но переделать использование |
| `DictionaryPractice.tsx` | #6, #10 | Проверить useEffect логику |
| `vocabulary_service.py` | #7 | Исправить logger.info() |
| `dictionary/repeat.tsx` | Нет | Проверить после исправлений |

---

## 🚀 СЛЕДУЮЩИЕ ШАГИ

1. **НЕМЕДЛЕННО:** Исправить ошибки #1, #4, #5 (удалить loadWords вызовы)
2. **СРОЧНО:** Переделать useDictionaryWords.ts (#3)
3. **ВАЖНО:** Проверить DictionaryPractice.tsx (#6)
4. **ОБЯЗАТЕЛЬНО:** Тестировать после каждого исправления
5. **ОПЦИОНАЛЬНО:** Добавить кэширование и race condition защиту

---

## 🧪 КАК ТЕСТИРОВАТЬ ИСПРАВЛЕНИЯ

**Сценарий 1:** Открыть урок с флэшкарточками
- Проверить: слова добавлены в словарь (1 запрос к backend)
- Проверить: новые слова отображаются в /dictionary
- Проверить: нет дублей

**Сценарий 2:** Открыть 2 урока подряд
- Проверить: только 2 запроса (по одному на урок)
- Проверить: словарь содержит слова из ОБОИХ уроков
- Проверить: нет дублей

**Сценарий 3:** Открыть урок в preview режиме
- Проверить: новых слов НЕ добавлено
- Проверить: GET /api/dictionary НЕ вызывается

**Сценарий 4:** Упражнения в словаре
- Проверить: markSuccess() обновляет счетчик
- Проверить: прогресс синкируется с backend
