{
  "name": "qazaq-platform",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "admin-spa"
  ],
  "scripts": {
    "build": "npm run build --workspace admin-spa",
    "dev": "npm run dev --workspace admin-spa",
    "lint": "npm run lint --workspace admin-spa"
  }
}
